boto3>=1.34
pydantic>=2.0
python-dotenv>=1.0

# Step 2 — Anthropic Python SDK (was openai-compatible client against Groq)
anthropic>=0.43

# Step 5 / Step 6 — Claude Agent SDK. Currently used only by the
# files under src/mcp_servers/ and the (preview) src/hooks_sdk.py.
# agent.py still works without it installed.
claude-agent-sdk>=0.2

# Step 5 — base MCP server framework (pulled in by claude-agent-sdk,
# pinned here for clarity).
mcp>=1.0
